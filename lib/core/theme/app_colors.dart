import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette (Trust & Education)
  static const Color primary = Color(
    0xFF6A9C89,
  ); // Sage Green - Calming & Professional
  static const Color primaryLight = Color(0xFFC4DAD2);
  static const Color primaryDark = Color(0xFF16423C);

  // Secondary Palette (Warmth & Energy)
  static const Color secondary = Color(0xFFFFB4A2); // Soft Apricot
  static const Color secondaryLight = Color(0xFFFFDBCF);
  static const Color accent = Color(0xFFE5989B); // Muted Pink

  // Backgrounds & Surfaces
  static const Color background = Color(0xFFF7F9F9); // Very subtle cool grey
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF0F2F5);

  // Text Colors
  static const Color textPrimary = Color(
    0xFF2D3436,
  ); // Charcoal - softer than black
  static const Color textSecondary = Color(0xFF636E72);
  static const Color textLight = Color(0xFFB2BEC3);

  // Status Colors
  static const Color success = Color(0xFF81B29A);
  static const Color warning = Color(0xFFF2CC8F);
  static const Color error = Color(0xFFE07A5F);
  static const Color info = Color(0xFF3D405B);
}
